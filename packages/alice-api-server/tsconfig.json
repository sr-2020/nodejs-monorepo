{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // TODO: Figure out what prevents enable it and remove
    "noImplicitAny": false,
  },
  "include": ["**/*.ts"],
  "references": [{"path": "../interface"}]
}
