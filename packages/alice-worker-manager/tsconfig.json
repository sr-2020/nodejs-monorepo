{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // TODO: Figure out what prevents enable it and remove
    "noImplicitAny": false,
    "strictFunctionTypes": false,
  },
  "include": ["**/*.ts"],
  "references": [
    {"path": "../alice-model-engine-api"},
    {"path": "../alice-model-engine"}
  ]
}
