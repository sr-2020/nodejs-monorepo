{
  "name": "sr2020-models-testing",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b",
    "test": "mocha --exit",
    "coverage:test": "nyc --cwd=.. --temp-dir=sr2020-models-testing/.nyc_output mocha --exit",
    "coverage:merge": "nyc merge .nyc_output ../../.nyc_output/sr2020-models-testing.coverage.json",
    "lint": "yarn run prettier:check && yarn run tslint",
    "lint:fix": "yarn run tslint:fix && yarn run prettier:fix",
    "prettier:cli": "prettier --ignore-path=../../.prettierignore  \"**/*.ts\"",
    "prettier:check": "yarn run prettier:cli -l",
    "prettier:fix": "yarn run prettier:cli --write",
    "tslint": "lb-tslint -c ../../tslint.json",
    "tslint:fix": "yarn run tslint --fix"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {},
  "dependencies": {
    "typeorm": "^0.2.18"
  }
}
